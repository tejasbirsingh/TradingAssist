[pytest]
# Confine collection to tests/. Without this, a bare `pytest` from the repo root
# also collects smoke_test.py, whose name matches `*_test.py` and which calls
# Breeze at import time — so the whole suite aborts during collection with
# "Session key is expired" whenever the daily token is stale.
testpaths = tests
